class Shop {
  constructor(newShop) {
    this.name = newShop.name;
    this.inventory = newShop.inventory;
  }
  addInventory(computer) {
    this.inventory = newShop.inventory === {};
  }
};








module.exports = Shop;
